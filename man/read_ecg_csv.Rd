% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_ecg_csv.R
\name{read_ecg_csv}
\alias{read_ecg_csv}
\title{Read an ECG signal from a CSV file}
\usage{
read_ecg_csv(
  file_path,
  time_col = NULL,
  signal_cols = NULL,
  fs = NULL,
  from_sec = 0,
  to_sec = NULL
)
}
\arguments{
\item{file_path}{Path to the CSV file.}

\item{time_col}{Name of the time column. If NULL, time is generated
automatically from \code{fs}.}

\item{signal_cols}{Character vector of signal column names to keep.
If NULL, all non-time numeric columns are used.}

\item{fs}{Sampling frequency in Hz. Required if \code{time_col} is NULL.
If \code{time_col} is provided, fs is inferred automatically.}

\item{from_sec}{Start time in seconds. Default 0.}

\item{to_sec}{End time in seconds. NULL = full file.}
}
\value{
An \code{ecg_df} object identical in structure to \code{read_ecg()}.
}
\description{
Reads a plain CSV file containing ECG data and returns the same
\code{ecg_df} format as \code{read_ecg()}, so all downstream
functions (plot_ecg, compute_hrv, plot_rr) work without any changes.
}
\examples{
\dontrun{
# CSV with a time column
df <- read_ecg_csv("my_ecg.csv", time_col = "time", signal_cols = "ecg")

# CSV with no time column, 500 Hz Apple Watch export
df <- read_ecg_csv("apple_watch.csv", signal_cols = "voltage", fs = 500)

# Then use exactly like MIT-BIH data
plot_ecg(df)
hrv <- compute_hrv(df)
}

}
